/**
 * App.js – MIRA root. Public form only, no patient list.
 */
import React from "react";
import PatientForm from "./PatientForm";

export default function App() {
  return (
    <div className="app-shell">
      <header className="site-header">
        <div className="header-inner">
          <div className="logo-block">
            <span className="logo-icon">⚕</span>
            <div>
              <h1 className="logo-title">MIRA</h1>
              <p className="logo-sub">Medical Intelligence &amp; Risk Assessment</p>
            </div>
          </div>
          <nav className="header-pills">
            <a href="/admin" className="pill pill--link">Admin Console →</a>
          </nav>
        </div>
      </header>

      <main className="main-single">
        <section className="panel panel--form">
          <h2 className="panel-title">+ Health Assessment</h2>
          <PatientForm />
        </section>
      </main>

      <footer className="site-footer">
        MIRA © {new Date().getFullYear()} — Health Prediction Platform —
        Predictions are generated by a local ML model and are for reference only.
        Always consult a qualified physician.
      </footer>
    </div>
  );
}
